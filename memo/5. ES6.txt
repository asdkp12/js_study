// 함수 선언문 (정의문) ';' 안찍음
// 먼저 호출하고 나중에 함수를 쓸 수 있음.

// 즉 함수도 하나의 데이터 이며 값이다.

// 즉 라이브러리 함수를 만들어서 쓸수있다.

// 익명 함수 : 함수를 값(리터럴)으로 본 경우
// 함수 표현식 ';' 찍음
// 먼저 호출하고 나중에 함수를 쓸 수 없음.

// ;를 찍고 값으로 저장.

// // 함수 선언문
// function add(n1, n2){
//     return n1 + n2;
// }

// // 함수 표현식
// const add = function(n1, n2){
//     return n1 + n2;
// };

// 화살표 함수 (그냥 표현식을 편하게 표현)